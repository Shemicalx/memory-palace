import React from 'react'

const Home = () => {

    return (
        <section>
            <h2>Welcome to the memory palace!</h2>
            <p>What will be on the main page is still unclear to me. Maybe a dashboard of sorts? Maybe an insentive to log in, and once logged in then a cool dashboard?</p>
        </section>
    )
}

export default Home
